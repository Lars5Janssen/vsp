services:
  sol:
    build: .
    entrypoint: app 
    depends_on:
      - component
    stdin_open: true
    tty: true
    networks:
      vs-network:

  component:
    build: .
    entrypoint: app -sleep -killSol
    stdin_open: true
    tty: true
    networks:
      vs-network:

networks:
  vs-network:
    external: false
